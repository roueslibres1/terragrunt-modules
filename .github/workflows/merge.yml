# Code generated by fabricator-generate-project-go
#
# Modifications in code regions will be lost during regeneration!
# region CODE_REGION(CI)
name: golang-build-merge
on:
  push:
    branches: [main]

env:
  # Golang version to use across CI steps
  GOLANG_VERSION: "1.17.6"

jobs:
  build-go:
    name: Build
    uses: roueslibres1/gh-actions/.github/workflows/generic-autotag.yml@main
    with:
      actions-ref: main
    secrets:
      token: ${{ secrets.CI_TOKEN }} #passes ci token since we want actions to trigger a workflows otherwise {{secrets.github-token}} would be enough
# endregion
